import React from 'react';
import GoogleMapWrapper from '../common/GoogleMapWrapper';

export const ProcurementMap = (props) => {
  return <GoogleMapWrapper {...props} />;
};

export default ProcurementMap;
